# Lens Golden Fixture 9 Pending Sample Intake Status v1

Date: 2026-05-11
Gate: Lens-Golden-Fixture-9
Status: blocked on actual sanitized sample files
Repo: admate-lens

## Purpose

Record the approved pending-sample intake gate for these Lens golden surfaces:

- `demandgen-youtube-feed`
- `kakao-bizboard`
- `naver-smart-channel-mobile`
- `youtube-shorts`

This gate is docs-only. It does not execute captures, open browser sessions,
upload files, download storage objects, clean storage, create PNGs, modify PNGs,
or promote binary assets.

## Files Inspected

- `tests/golden/manifests/demandgen-youtube-feed.json`
- `tests/golden/manifests/kakao-bizboard.json`
- `tests/golden/manifests/naver-smart-channel-mobile.json`
- `tests/golden/manifests/youtube-shorts.json`
- `tests/golden/metadata/demandgen-youtube-feed.json`
- `tests/golden/metadata/kakao-bizboard.json`
- `tests/golden/metadata/naver-smart-channel-mobile.json`
- `tests/golden/metadata/youtube-shorts.json`
- `docs/quality/golden-sample-policy.md`

## Current Contract State

All four inspected surfaces remain placeholder contracts:

| Surface | sampleState | sensitivity | fixtureKind | golden path present | candidate path present |
| --- | --- | --- | --- | --- | --- |
| `demandgen-youtube-feed` | `pending-sample` | `repo-safe-placeholder` | `placeholder` | no | no |
| `kakao-bizboard` | `pending-sample` | `repo-safe-placeholder` | `placeholder` | no | no |
| `naver-smart-channel-mobile` | `pending-sample` | `repo-safe-placeholder` | `placeholder` | no | no |
| `youtube-shorts` | `pending-sample` | `repo-safe-placeholder` | `placeholder` | no | no |

No repo-safe sample image files are present at the manifest-declared image
paths:

```text
tests/golden/images/demandgen-youtube-feed/golden.png
tests/golden/images/kakao-bizboard/golden.png
tests/golden/images/naver-smart-channel-mobile/golden.png
tests/golden/images/youtube-shorts/golden.png
```

No repo-local candidate image files are present at:

```text
tests/golden/candidates/demandgen-youtube-feed/current.png
tests/golden/candidates/kakao-bizboard/current.png
tests/golden/candidates/naver-smart-channel-mobile/current.png
tests/golden/candidates/youtube-shorts/current.png
```

## Decision

Result: APPROVAL RECEIVED, BLOCKED ON ACTUAL SANITIZED SAMPLE FILES

The intake gate can acknowledge approval intent for the pending-sample queue,
but it cannot safely promote any of these four surfaces to `approved` until the
actual sanitized PNG files and matching approved metadata are provided in the
repository.

The current safe state is unchanged:

```text
sampleState: pending-sample
sensitivity: repo-safe-placeholder
fixtureKind: placeholder
local pixel diff: skipped
```

## Exact Required Inputs

For `demandgen-youtube-feed`, provide:

```text
target golden path: tests/golden/images/demandgen-youtube-feed/golden.png
metadata path: tests/golden/metadata/demandgen-youtube-feed.json
expected dimensions: 1170x2532
required productFamily: demand-gen
required productSurface: youtube-feed
required youtubeAdType: mobile-infeed-home
required resultCategory: ad_capture_ok
candidate generated by: AdMate Lens
manual image editing: no
sensitivity: repo-safe
approval: commit this binary PNG as a repo-safe golden asset
```

For `kakao-bizboard`, provide:

```text
target golden path: tests/golden/images/kakao-bizboard/golden.png
metadata path: tests/golden/metadata/kakao-bizboard.json
expected dimensions: 1179x2556
required productFamily: kakao
required productSurface: kakao-bizboard
required resultCategory: ad_capture_ok
candidate generated by: AdMate Lens
manual image editing: no
sensitivity: repo-safe
approval: commit this binary PNG as a repo-safe golden asset
```

For `naver-smart-channel-mobile`, provide:

```text
target golden path: tests/golden/images/naver-smart-channel-mobile/golden.png
metadata path: tests/golden/metadata/naver-smart-channel-mobile.json
expected dimensions: 1179x2556
required productFamily: naver
required productSurface: naver-smart-channel-mobile
required resultCategory: ad_capture_ok
candidate generated by: AdMate Lens
manual image editing: no
sensitivity: repo-safe
approval: commit this binary PNG as a repo-safe golden asset
```

For `youtube-shorts`, provide:

```text
target golden path: tests/golden/images/youtube-shorts/golden.png
metadata path: tests/golden/metadata/youtube-shorts.json
expected dimensions: 1170x2532
required youtubeAdType: shorts-feed
required resultCategory: ad_capture_ok
candidate generated by: AdMate Lens
manual image editing: no
sensitivity: repo-safe
approval: commit this binary PNG as a repo-safe golden asset
```

Every supplied sample also needs confirmation that it contains no advertiser,
campaign, account, private URL, credential, token, personal, or private landing
data.

## Not Changed

This gate intentionally does not change:

- `tests/golden/manifests/**`
- `tests/golden/metadata/**`
- `tests/golden/images/**`
- `tests/golden/candidates/**`
- `tests/golden/diffs/**`
- `tests/golden/reports/**`
- capture engine, renderer, composite, injection, API, DB, schema, env, storage,
  browser session, upload, or cleanup logic

## Validation

Safe validation for this docs-only gate:

```text
git diff --check -- docs/tasks/2026-05-11_lens_golden_fixture_9_pending_sample_intake_status_v1.md
npm run check:golden-manifest
npm run check:golden-metadata
npm run check:golden-dimensions
```

`npm run verify:golden` is not safe for this restricted gate because it invokes
`check:golden-pixels`, which writes diff PNGs and report JSON for approved local
goldens. This gate is explicitly not allowed to create or modify PNG files.

## No-Touch Confirmation

This gate did not perform:

- capture execution
- browser/session work
- upload
- storage object download
- storage cleanup
- golden PNG creation, modification, or promotion
- candidate PNG creation or modification
- diff PNG creation or modification
- manifest or metadata mutation
- product asset change
- production API calls
- secret, env, token, cookie, credential, signed URL, raw provider response, or
  raw capture payload output
