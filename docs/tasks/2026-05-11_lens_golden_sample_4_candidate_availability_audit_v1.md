# Lens Golden Sample 4 Candidate Availability Audit v1

Date: 2026-05-11
Status: blocked / no-op
Scope: approved sample candidate availability audit

## Verdict

Decision: BLOCKED FOR SAMPLE CANDIDATE

The next asset-intake gate cannot proceed yet because no exact repo-safe golden
sample candidate is available in the repository, and no reviewer-supplied
candidate PNG plus matching sanitized metadata was provided for this gate.

The current repository state remains the safe placeholder state:

- `6` golden manifests exist.
- All `6` manifests remain `pending-sample`.
- `6` metadata fixtures exist.
- No approved golden PNG was added.
- No candidate PNG was added.
- No pixel validation was run.

## Current Placeholder Surfaces

Pending surfaces:

- `demandgen-youtube-feed`
- `gdn-pc-display`
- `kakao-bizboard`
- `naver-smart-channel-mobile`
- `youtube-pc-instream-skip`
- `youtube-shorts`

## Candidate Review

Current in-repo files are not sufficient to approve a golden baseline:

- Existing golden manifests and metadata are placeholders only.
- Prior visual-QA evidence PNGs are QA evidence, not golden sample candidates.
- Public frame assets are product/frame assets, not generated Lens capture
  outputs.
- No file under `tests/golden/images` is available for approval.
- No file under `tests/golden/candidates` is available for review.

The policy still requires an exact sample candidate and explicit asset review
approval before changing a manifest from `pending-sample` to `approved` or
adding a committed golden PNG.

## Verification

Commands run:

```text
npm run verify:harness
npm run check:golden-manifest
npm run check:golden-metadata
npm run check:golden-dimensions
npm run harness:report
```

Results:

- `verify:harness`: pass
- `check:golden-manifest`: pass, `6` manifests, `6` pending samples
- `check:golden-metadata`: pass, `6` metadata fixtures
- `check:golden-dimensions`: pass, `0` checked, `6` skipped
- `harness:report`: pass, no golden errors

## No-Touch Confirmation

This gate did not:

- add or modify golden PNGs
- add or modify product assets
- add or modify generated candidates
- add or modify generated diffs
- add or modify generated reports
- change manifests or metadata fixtures
- run pixel validation
- run capture
- run upload
- start browser capture sessions
- write DB rows
- write storage objects
- change DB, schema, env, or storage policy
- modify capture engine, renderer, composite, injection, or login code
- run cleanup or delete commands

## Required Input For Next Asset Gate

To proceed beyond this blocked audit, a reviewer must provide or approve:

- exact surface id
- exact repo-safe PNG candidate path
- matching sanitized metadata fixture updates
- evidence that the PNG is generated by AdMate Lens, not manually edited
- dimensions matching the manifest
- confirmation that the image contains no advertiser, campaign, account, URL,
  credential, personal, or private landing data
- explicit approval to stage binary golden assets and run pixel validation

## Next Gate

Recommended next gate only after the required input exists:

```text
Gate Lens-Golden-Sample-5 Approved Repo-Safe Sample Intake
```

Until then, the golden sample set should remain in `pending-sample` placeholder
mode.
